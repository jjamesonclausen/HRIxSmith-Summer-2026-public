

**To-Dos**

1. Finalize the VAWT design goal
   - Define exactly what we are trying to design.
   - Determine success criteria for the project.
   - Decide whether location assumptions (such as using the Boston area wind profile) will be included.
2. Create a draft project work plan
   - Generate `3-5` AI-created project plans with different assumptions.
   - Compare plans based on:
     - timeline
     - available tools
     - desired deliverables
     - available personnel
   - Combine the best parts of each into one final plan.
3. Test CSV to JSON conversion
   - Convert existing metadata CSV files into JSON.
   - Run the wiki ingestion pipeline again.
   - Compare:
     - metadata completeness
     - metadata accuracy
     - consistency of extraction
   - Determine whether JSON solves the metadata issue.
4. Build reusable prompt and planning assets
   - Create:
     - `/plans` folder
     - `/prompts` folder
     - Markdown files containing:
       - project constraints
       - design requirements
       - desired outputs
       - prompting templates
5. Explore Zookeeper CAD workflow
   - Read documentation.
   - Learn best prompting practices.
   - Test: source-code interface, GUI interface, agentic editor
     - Determine which interface works best for each task.

**Deliverables for Next Meeting**

- Final design objective
- Draft work plan created from multiple AI-generated options
- Results of JSON metadata testing
- Initial Zookeeper exploration report
- Initial SimScale exploration report
